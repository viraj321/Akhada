package com.example.akhada.entity.components;

public enum FighterState {
    STANDING,   // balance correction active, resists gravity
    RAGDOLL     // pure physics, no correction — this is what you already built
}
